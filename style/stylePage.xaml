<?xml version="1.0" encoding="utf-8"?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
		xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
		xmlns:local="clr-namespace:style" x:Class="style.stylePage"
		Style="{DynamicResource Background}"
		>
	<StackLayout VerticalOptions="CenterAndExpand" HorizontalOptions="CenterAndExpand" >
		<Label Text="Welcome to Xamarin Forms!" VerticalOptions="Center" HorizontalOptions="Center" />
		<Label Text="titleStyle titleStyle titleStyle titleStyle titleStyle titleStyle titleStyle" Style="{DynamicResource Title}" />
		<Label Text="detailStyle" Style="{DynamicResource Detail}"  />
		<Label Text="captionStyle" Style="{DynamicResource Caption}" />
		<Entry x:Name="color" Placeholder="Color" BackgroundColor="Gray" />
		<Entry x:Name="lineEndMode" Placeholder="LineEndMode" BackgroundColor="Gray" />
		<Entry x:Name="fontSize" Placeholder="FontSize" BackgroundColor="Gray" />
		<Button x:Name="change" Text="change" BackgroundColor="Gray" Style="{DynamicResource ButtonStyle}" />
	</StackLayout>
</ContentPage>
